import { PortfolioElement } from './types';

const portfolioElements: PortfolioElement[] = [
  {
    id: 8,
    title: "Whisper",
    type: "Personal",
    skills: ["Rust", "Cloud Architecture", "Axum", "Clap", "PostgreSQL", "AES-256-GCM", "PBKDF2", "SQLx", "Askama", "Docker", "TypeScript", "Discord.js", "Raycast API", "npm", "GitHub Actions", "CI/CD", "Cross-compilation", "Cucumber/BDD", "Systemd"],
    description: "Open-source, zero-knowledge secret manager I designed, built, and ship as both a Rust web app (whisper.quentinvedrenne.com) and a multi-platform CLI distributed on npm (whisper-secrets). Two products on one Rust core: one-time URL-based secret sharing, and a CLI that encrypts your team's .env client-side before it ever touches the network. Published on npm via OIDC Trusted Publishing with SLSA provenance, integrated with Slack, Discord, and a Raycast extension merged upstream into raycast/extensions. Self-hostable, with a hexagonal Rust architecture, full GitHub Actions CI/CD, and a public Slack community.",
    detail: [
      { id: 1, title: "Two products built on one Rust core", task: ["Web app — one-time secret URL with auto-expiration (up to 7 days) and optional self-destruct on first read", "CLI (whisper-secrets, alias ws) — team .env workflow: init / push / pull / rotate / remove / status / invite / join, plus interactive picker for untracked entries and clipboard auto-copy on share", "Both share the same Rust domain core; only the I/O adapters differ", "Fully self-hostable — same binary anyone can run with Docker Compose + PostgreSQL"] },
      { id: 2, title: "Hexagonal architecture & DDD in Rust", task: ["Cargo workspace: services-core (pure domain — no async, no I/O), adapters (postgresql-adapter, aes-gcm-crypto), applications (axum/server, cli, discord)", "Ports & adapters — domain depends on traits (SharedSecretRepository, SecretEncryption); adapters implement them, so swapping PostgreSQL or the crypto backend is a new crate, not a rewrite", "Domain modelled with entities, value objects (SecretId, SecretEncrypted, SecretExpiration), commands, and contract traits"] },
      { id: 3, title: "Zero-knowledge encryption with a layered threat model", task: ["CLI: passphrase → PBKDF2-SHA256 at 600,000 iterations (matches the OWASP 2023 minimum) → both an AES key and an auth token", "AES-256-GCM authenticated encryption with a unique random 12-byte nonce per secret — plaintext never leaves the client", "Defense in depth: managed-secret uploads are encrypted again server-side with a separate file-loaded AES key — compromising one layer doesn't reveal plaintext", "Atomic DELETE/UPDATE … RETURNING in PostgreSQL eliminates race conditions on self-destruct reads"] },
      { id: 4, title: "Distributed as a first-class npm package (whisper-secrets)", task: ["Single `npm i -g whisper-secrets` resolves the correct native binary via platform-scoped optionalDependencies (linux-x64, linux-arm64, darwin-arm64, win32-x64)", "Publishing uses npm OIDC Trusted Publishing — no NPM_TOKEN in repo or CI; every release ships with --provenance / SLSA attestations", "Also installable via a one-line shell installer (curl | sh) or signed pre-built binaries from GitHub Releases", "CLI itself: clap for parsing, dialoguer + indicatif for interactive UX, anonymous Mixpanel telemetry (command, success, version, OS, arch) with DO_NOT_TRACK=1 opt-out"] },
      { id: 5, title: "Native integrations on Slack, Discord, and Raycast", task: ["Slack: /whisper slash command with HMAC-SHA256 signature verification middleware and ephemeral responses", "Discord: standalone bot in TypeScript + discord.js with /whisper slash command, configurable duration & self-destruct", "Raycast: native macOS extension I authored that was merged upstream into raycast/extensions (PR #25802) — quick command + form mode, auto-copies share link to clipboard"] },
      { id: 6, title: "Full GitHub Actions CI/CD across three workflows", task: ["whisper-ci — every PR: rustfmt --check, clippy --all-targets -D warnings, workspace unit + bin tests, plus 6 Gherkin/Cucumber BDD feature files covering shared & managed secret lifecycles", "whisper-cli-release — tag-triggered matrix build across 4 targets (Linux x64/arm64 via cross, macOS arm64, Windows x64); strips binaries, uploads .tar.gz / .zip + SHA256 to GitHub Releases, publishes 4 platform npm packages + the base whisper-secrets package with provenance", "whisper-release — server pipeline auto-deploys to an Azure VM via SCP and restarts the systemd unit", "Tag version is validated against Cargo.toml before any artifact is built — no mismatched releases possible"] },
      { id: 7, title: "Production hardening & OSS maturity", task: ["Security headers via tower-http: HSTS, CSP, X-Frame-Options, X-Content-Type-Options on every response", "Compile-time-checked SQL via SQLx (sqlx::query!) — SQL injection caught at build time", "Askama templates with auto-escaping — zero XSS surface; rust-embed bundles CSS/SVG into the binary for single-file deploys", "tokio-cron-scheduler runs an expired-secret sweep every minute; generic errors to clients, full errors logged server-side", "Public Slack community, badges (CI / npm version / downloads / license), CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md, and issue / PR templates"] },
    ],
    img: ["/whisper_0.png", "/whisper_1.png"],
    github: ["https://github.com/quentinved/whisper", "https://github.com/raycast/extensions/pull/25802"],
    titleref: "https://whisper.quentinvedrenne.com/",
  },
  {
    id: 13,
    title: "MixBar",
    type: "Personal",
    skills: ["Swift 6", "SwiftUI", "Core Audio", "AppKit", "macOS", "Hexagonal Architecture", "Swift Package Manager", "Realtime Audio", "Astro", "Cloudflare", "GitHub Actions", "CI/CD", "Code Signing & Notarization", "Open Source"],
    description: "Open-source per-app volume mixer for the Mac menu bar, which I designed, built and ship (mixbar.quentinvedrenne.com) — a slider for every application making sound, plus one-click output-device switching. macOS has no API for per-app volume, so MixBar captures each app's audio with Core Audio process taps, applies your gain and mixes it back out in real time. It needs no kernel extension and no virtual audio driver, and at default volume it stays completely out of your audio path. Free, MIT-licensed, and distributed as a signed and notarized .dmg for macOS 15+.",
    detail: [
      { id: 1, title: "How per-app volume is done without a driver", task: ["Built on Core Audio process taps, introduced in macOS 14.4 — the first public way to read another app's audio without a kernel extension or a virtual output device", "Enumerate audio processes through AudioHardwareSystem.shared.processes", "Group them by owning application, because browsers and Electron apps play through anonymous helper processes rather than themselves", "For an adjusted app, create one CATapDescription spanning all of its processes, marked isPrivate with muteBehavior = .mutedWhenTapped — that silences the app's own path to the speakers while it is read, so our gain becomes its volume", "Put the default output device and every tap into one private aggregate device and run a single IOProc that applies per-app gain and sums the result"] },
      { id: 2, title: "Nothing in the audio path until you ask", task: ["At default volume MixBar creates no taps and no aggregate device at all — it is simply not between your apps and your speakers", "An app enters the audio path the moment its slider moves and leaves the moment it returns to 100%", "This is the whole design, not an optimisation: an earlier version routed everything through the mixer and caused audible crackling system-wide, even while mixing pure silence"] },
      { id: 3, title: "The permission, explained rather than requested", task: ["Apple classifies reading another app's sound as recording, so MixBar needs Audio Recording (kTCCServiceAudioCapture) even though it never touches the microphone", "A missing grant is silent by design in Core Audio — the tap is created, the render callback runs, and every buffer is zeros, with no error to surface — so the app detects the case and links to the right System Settings pane", "Buffers are multiplied by a gain, summed and written to the output device. Nothing is recorded, written to disk or sent anywhere; there is no network code in the app"] },
      { id: 4, title: "Hexagonal, down to the realtime thread", task: ["The Domain (AudioApplication, AppMix, MixerLayout, MixerSnapshot, MixerService) imports Foundation and nothing else — the mixing rules hold no framework types", "Driven ports for catalog, engine, outputs and settings; one driving port, MixerControlling, is all the UI may touch", "ProcessRegistry is the seam that makes it work: it maps the domain's AudioAppID to the several Core Audio process objects behind it, so AudioObjectID never appears above the adapter layer", "RealtimeMixer is the IOProc — no allocation, no locks, and never holding a Swift reference", "Every port has a fake, so 37 tests run the rules with no audio device, no output and no permission prompt"] },
      { id: 5, title: "The interface", task: ["Three layouts from one segmented control: Compact (one line per app), Comfortable (icon, name, full-width slider) and Mixer (vertical faders, like a desk)", "The bright band inside each slider is a live level meter read after your gain is applied, so it can never exceed the fill — what you see is what reaches the speakers", "Per-app volumes are remembered by bundle ID, so quitting Spotify and reopening it keeps the level", "Output device switching from the top of the popover, mute per row, and one reset for everything"] },
      { id: 6, title: "Shipped, signed and notarized from CI", task: ["Tag a release and GitHub Actions builds a universal binary, signs it with the Developer ID certificate, notarizes it with Apple and staples the ticket — the .dmg on the release page is never produced by hand", "Also installable as a Homebrew cask, with a zap list so uninstalling leaves nothing behind", "Tests run in CI on every push, with no audio hardware required", "SwiftPM and the macOS SDK only — no Xcode project file, no third-party dependencies", "The version in Info.plist is checked against the tag before anything is built, so a release can never ship the previous build"] },
      { id: 7, title: "A landing page that cannot lie about the app", task: ["mixbar.quentinvedrenne.com is an Astro site living in the same repository, deployed on Cloudflare", "It reads the version straight out of Info.plist and counts the @Test cases under app/Tests at build time, so the numbers on the page are the ones in the code — the build fails rather than publishing a stale claim", "Screenshots are the app's real captures, in light and dark, shared with the README", "MIT licensed, with CONTRIBUTING.md covering the build loop, the one architectural rule and the Core Audio traps that otherwise cost an afternoon, plus a code of conduct and a private security policy", "Documented properly where it matters: an unbundled binary never gets the permission prompt, so the build script produces a real .app and the README says why swift run is not enough"] },
    ],
    img: ["/mixbar_0.jpg", "/mixbar_1.jpg"],
    github: ["https://github.com/quentinved/MixBar"],
    titleref: "https://mixbar.quentinvedrenne.com/",
  },
  {
    id: 12,
    title: "App Uninstaller",
    type: "Personal",
    skills: ["TypeScript", "React", "Raycast API", "Node.js", "macOS", "AppleScript", "Spotlight", "TCC", "Unit Testing"],
    description: "A Raycast extension that removes a macOS application and the files it leaves behind — caches, containers, preferences, launch agents, privileged helpers — after showing you exactly what it found and why. Most uninstallers delete anything whose name resembles the app, which is how people lose unrelated data. This one is built the other way round: every match carries its evidence, weak evidence stays unselected, and nothing is deleted — it goes to the Trash. Submitted to the Raycast store (PR open on raycast/extensions).",
    detail: [
      { id: 1, title: "Evidence, not resemblance", task: ["Certain — the file is named after the bundle identifier, including Team-ID group containers; Likely — named exactly after the app; Unsure — same developer prefix, or the name merely appears. Only the first two are pre-selected", "Names too generic to identify anything (Helper, Updater, Desktop, Code…) are never matched on; for those apps only the bundle identifier counts", "Contested files are demoted: every candidate is tested against every installed app, dropped if another matches it more strongly, and labelled \"Also matches X\" if another matches it equally", "Folders belonging to another app are not searched, and two levels down a resemblance-only match is discarded entirely"] },
      { id: 2, title: "A path guard written for the day the matcher is wrong", task: ["An allow-list, not a deny-list: a path is removable only if it resolves inside a known application directory, is not that directory itself, and is no deeper than that directory permits", "The parent of each path is resolved before the check, so a symlinked folder inside a search directory cannot redirect a deletion elsewhere", "Shared and system directories are protected — Application Support/Google is never removable, only one app's folder inside it", "The guard runs twice: once while scanning, and again inside the only function that deletes, immediately before it acts. Covered by tests, including a live symlink-escape attempt"] },
      { id: 3, title: "Privileged removals, kept at arm's length", task: ["Root-owned items — every App Store application, and anything under /Library — are separated out and never included in an ordinary removal", "Removing them is a distinct action on an explicit selection that raises macOS's own password dialog; the extension never sees the password and never silently retries as root", "Paths reach the privileged command as argv, quoted by AppleScript's quoted form of, so a filename containing $(…), ; or quotes is one literal argument to mv", "A privileged move lands in a new, empty, user-owned folder in the Trash named after the app, so it can never overwrite a same-named item already there"] },
      { id: 4, title: "Permissions asked for before they can fail silently", task: ["Full Disk Access and App Management are both refused silently by macOS, so the extension checks them up front, links straight to each pane, re-reads every 1.5 seconds and closes itself the moment they are granted", "Full Disk Access is tested directly — a file only that permission can open is opened and immediately closed, with no bytes read", "App Management has no safe probe (every one would modify an application bundle), so the answer is read back from the TCC database: read-only, one constant query, one table, only the rows for that permission", "That record is itself behind Full Disk Access, so without it App Management is reported as Unknown rather than assumed either way"] },
      { id: 5, title: "Failures that explain themselves", task: ["macOS reports a refused removal as a bare \"could not be trashed\", so the extension diagnoses it from the path and puts the right System Settings pane on the failing row as its first action", "Detects the case that looks like a permission problem and is not: moving a root-owned bundle rewrites its .. entry, which needs write permission on the directory itself — no privacy toggle can grant that", "Detects an app extension still loaded in its host (a Safari appex holding the bundle open), names the process and offers to quit it, rather than blaming a permission that is already granted", "Nothing fails silently: every failure stays in the list with its path and reason, retryable with ⌘Y, with a copyable mv command as a fallback"] },
      { id: 6, title: "Lists that answer the question you actually have", task: ["By Size — banded so the apps worth reclaiming lead the list", "By Last Used — read from Spotlight where it knows (about a quarter of installed apps), otherwise estimated from when the app last wrote its preferences, saved state or container. Each row says which of the two it is, because an estimate is not a launch date", "Detects Homebrew casks, vendor uninstallers and installer receipts, and says when one of those is the better tool — brew uninstall --cask --zap knows the app's own cleanup list", "No network access and no telemetry: the extension makes no outbound connections of any kind"] },
    ],
    img: ["/uninstaller_0.jpg", "/uninstaller_1.jpg"],
    comingSoon: "Waiting on review in the Raycast store",
    comingSoonUrl: "https://github.com/raycast/extensions/pull/31245",
    comingSoonUrlLabel: "Read the open PR \u2197",
    github: ["https://github.com/raycast/extensions/pull/31245"],
  },
  {
    id: 9,
    title: "Edamame Technologies",
    type: "Professional",
    skills: ["Rust", "AWS", "React", "Flutter", "Serverless", "DynamoDB", "CloudFormation", "CloudFront", "Lambda", "OAuth 2.0", "GraphQL", "REST", "Hexagonal Architecture", "GitHub Actions", "CI/CD", "CloudWatch", "Sentry"],
    description: "My day job — founding engineer at Edamame Technologies, a device-security startup. I build the serverless React + Rust platform that lets CISOs analyze device security postures and enforce access control on GitHub, Azure, and Google through custom policies — and I lead the 4-engineer team shipping it.",
    detail: [
      { id: 1, title: "Security-posture platform (React + Rust on AWS)", task: ["Serverless dashboard where CISOs analyze device security scores reported by our Flutter agent", "Custom-policy access control enforced across GitHub, Azure, and Google", "Rust backend structured with hexagonal architecture for maintainability and team collaboration"] },
      { id: 2, title: "Rust engine for dynamic IP allow-lists", task: ["Dynamic IP allow-list management with OAuth 2.0 access control", "Exposed over both REST and GraphQL APIs"] },
      { id: 3, title: "AWS infrastructure & observability", task: ["DynamoDB, CloudFormation, CloudFront, and Lambda — the whole stack defined as code, designed to scale from zero", "Increased scalability by 50% and cut operational costs by 30%", "CloudWatch dashboards for invocations, error rates, p95 latency, and DynamoDB throttling; Sentry integrated across web, mobile, and Rust services"] },
      { id: 4, title: "Flutter agent performance", task: ["Introduced multithreading through a Rust FFI backend, improving end-to-end performance by ~30%"] },
      { id: 5, title: "Team & engineering culture", task: ["Recruited and led a team of four engineers — interviews, onboarding, and standards", "Designed the GitHub Actions CI/CD pipeline used by every service: fmt, lint, test, build, deploy on each PR", "Standardized GitHub workflows (PR templates, branch protections, automated reviewers)"] },
    ],
    img: ["/edamame_0.png", "/edamame_1.png", "/edamame_2.png"],
    titleref: "https://edamame.tech",
  },
  {
    id: 11,
    title: "Who's First",
    type: "Personal",
    skills: ["Swift", "SwiftUI", "UIKit", "iOS", "iPadOS", "Hexagonal Architecture", "Game Center", "XCTest", "XCUITest", "CryptoKit", "Cloudflare Workers", "App Store Connect API", "GitHub Actions", "CI/CD"],
    description: "A finger picker and team maker for iPhone and iPad, submitted to the App Store as Who's First: Finger Picker. Everyone puts a finger on the screen; after a short countdown the app picks one — or splits the group into two balanced teams. It is a deliberately small app built properly: a FingrCore package that imports neither SwiftUI nor UIKit, every external thing behind a port, and a test suite that plays whole rounds with a fake clock and a deterministic random source. No ads, no purchases, no accounts, no third-party dependencies.",
    detail: [
      { id: 1, title: "A round, precisely defined", task: ["Fingers land on the board and each gets a numbered, coloured ring; the countdown starts on its own once enough people have joined", "Joining or lifting a finger restarts the countdown; moving one does not", "The draw is uniform and without replacement — the spotlight racing between the rings is decoration, and the result is decided independently of it", "Team mode splits everyone in two, larger team first when the count is odd", "Opening the options sheet or backgrounding the app cancels the countdown, so a winner can never be picked while nobody is looking"] },
      { id: 2, title: "Multi-touch through a thin UIKit adapter", task: ["SwiftUI throughout, with a UIKit surface underneath purely for real multi-touch", "Each touch is forwarded as a batched domain input with coordinates normalised to the play area, so a finger keeps its identity and position for the whole round", "Tested with two-finger input synthesised by XCTest, plus full-screen touch coverage"] },
      { id: 3, title: "Ports and adapters, in an app this size", task: ["Packages/FingrCore holds Round (the rules) and GameSession (the countdown) and imports no UI framework at all", "PreferencesPort → UserDefaults, FeedbackPort → haptics and accessibility, SoundtrackPort → AVAudioPlayer, AchievementsPort → GameKit, CountdownClock → a cancellable system clock, RandomSource → the system generator", "Composition root wires the production adapters in one place; the tests wire fakes instead", "Game Center is best effort — a player who never signs in gets exactly the same game, and the tally is sent up whenever it next becomes reachable"] },
      { id: 4, title: "Tests that do not need a simulator to be meaningful", task: ["Core suite: exact winner counts, distinct selections, uniform team assignment, balanced groups from two to ten players, pausing, cancellation, replay, music timing, preference migration", "The countdown is driven by an injectable clock the tests advance by hand — no sleeping in tests", "UI suite covers launch, options, demo and replay, persistence across relaunch, multiple winners, a six-player team round and quick dares", "ScreenshotTests drives the six App Store marketing states, so the store screenshots are a build artifact rather than a manual capture"] },
      { id: 5, title: "Every asset produced by a script", task: ["App icon and achievement badges drawn by Swift scripts", "The three audio cues synthesised by a Python script using the standard library only", "Store frames and the marketing banner composited from the real captures and the shipped icon, so neither can drift from the app", "Audio uses the ambient session category: it respects Silent mode, mixes with whatever else is playing, and stops on interruption rather than resuming later"] },
      { id: 6, title: "Store tooling written from scratch", task: ["Small Swift command-line tools share an App Store Connect client that signs its own JWT with CryptoKit — listing, screenshots and Game Center achievements are all pushed from the repo", "A two-page Cloudflare Worker serves the privacy policy and support URLs the store requires", "Recovering a stuck review submission via the API — clearing rejected submission items and re-asserting the build — is documented in the repo, because the App Store Connect UI reports it as an invalid binary and nothing about it says otherwise"] },
    ],
    img: ["/whosfirst_0.jpg", "/whosfirst_1.jpg", "/whosfirst_2.jpg"],
    comingSoon: "Waiting for App Store approval",
    github: ["https://github.com/quentinved/whos-first"],
  },
  {
    id: 10,
    title: "Scopa Bella",
    type: "Personal",
    skills: ["Swift 6", "SwiftUI", "iOS", "Hexagonal Architecture", "Domain-Driven Design", "Game Center", "Multipeer Connectivity", "WebSockets", "Cloudflare Workers", "D1", "Durable Objects", "TypeScript", "XCTest", "GitHub Actions", "CI/CD", "App Store Connect API", "Localization"],
    description: "An iOS take on Scopa, the card game Italy plays at the kitchen table. Play the phone, pass it around the table, connect the phones in the room, or play strangers on a ranked ladder where everybody gets the same daily deal. Swift 6 and SwiftUI on top of dependency-free Swift packages, with a single Cloudflare Worker behind the online parts. Four completely different ways to play a table — hot-seat, Multipeer, Game Center, relay rooms — all drive the same coordinator through one transport port.",
    detail: [
      { id: 1, title: "One game core, four transports", task: ["ScopaCore/Domain is pure functions over value types: Rules validates and applies a move, Scoring settles a round, Search is the strongest bot — no async, no I/O, no UIKit", "A HostCoordinator owns the authoritative GameState and sends each seat only its own PlayerView; a GuestClient mirrors one seat", "Both talk through a GameTransport port, so hot-seat, Multipeer Connectivity, Game Center and WebSocket relay rooms are four adapters rather than four codebases", "The app layer only ever sees TableUpdate values — it cannot reach the game state directly"] },
      { id: 2, title: "Bots that only see what a player would see", task: ["Three strengths, each restricted to the information available from that chair — no peeking at the deck", "The hard bot samples the unseen cards and plays the round out, so it counts the deck the way a strong human does", "Tested by dealing identical cards to two levels and checking the stronger one wins more often — a real edge rather than a lucky shuffle"] },
      { id: 3, title: "A coach that explains the game while you play", task: ["Live explanation of each move as it happens, in the language of the rules rather than of the code", "A post-game review that grades every move you made", "Classic rules for two, three or four players, with teams at four"] },
      { id: 4, title: "Ranked ladder on one Cloudflare Worker", task: ["One Worker, one D1 database, one Durable Object class for rooms — inside the free tier at this scale", "The Worker verifies Game Center signatures itself, so a result can only ever be recorded for the player who actually produced it", "Monthly seasons with leagues and divisions, win streaks worth up to 10 extra points, and a house player seated when a ranked table waits too long — rationed, and never able to build or break a streak", "The app is fully playable offline; the Worker only adds comparison with other people"] },
      { id: 5, title: "Daily deal, challenges and a currency you cannot buy", task: ["A daily deal everyone plays on the same cards — identical luck, so only the play separates players", "Weekly challenges and twelve Game Center achievements", "Denari earned by playing and spent on card styles, cloths and reactions — no real-money purchases anywhere in the app"] },
      { id: 6, title: "Every asset generated from code", task: ["All music and sound effects synthesised by a SoundForge tool rather than licensed", "App icon and achievement artwork rendered by scripts, so they cannot drift from the app", "The App Store listing itself lives in the repo (StoreMetadata/copy.swift) in English, French and Italian, and is pushed to App Store Connect — the web form is the output, never the source"] },
      { id: 7, title: "Build tooling for many parallel sessions", task: ["Tools/dev.sh serialises builds and simulator use behind locks and shares one DerivedData, so several concurrent sessions queue instead of colliding", "Debug-only launch arguments (-noGameCenter, -denari, -shop, -autoPlay) to reach any state without playing to it", "CI on every push: the Swift package suite (rules, bots, coach, review, networking, rewards), the Worker suite (ranking, seasons, identity verification) and a build of the app", "Network tests run a host and several guests over an in-process loopback transport — no devices needed"] },
    ],
    img: ["/scopa_0.jpg", "/scopa_1.jpg", "/scopa_2.jpg"],
    comingSoon: "Not released yet",
    hidden: true,
  },
  {
    id: 0,
    title: "Serverless and AWS",
    type: "School",
    skills: ["AWS", "Cloud Architecture", "Serverless", "Go", "Vite.js", "Lambda", "DynamoDB", "S3", "API Gateway", "CloudFront", "CloudFormation", "Cognito", "SAM", "CloudWatch", "GitHub Actions", "CI/CD", "IaC"],
    description: "End-to-end serverless CRM built on AWS — a Go REST API on Lambda + API Gateway + DynamoDB, a Vite.js SPA served via CloudFront/S3, and Cognito for auth — with the entire infrastructure defined as code in CloudFormation and shipped through a GitHub Actions CI/CD pipeline. Zero servers to manage, observability via a CloudWatch dashboard.",
    detail: [
      { id: 1, title: "Go REST API on AWS Lambda", task: ["Full CRUD for articles (create / get / list / update / delete) plus tag-based search, each handler isolated as its own Lambda", "API Gateway in front for routing, throttling, and request validation", "DynamoDB single-table design for articles and tag indexes"] },
      { id: 2, title: "Vite.js SPA with Cognito auth", task: ["Sign-up / sign-in / token refresh flow against Amazon Cognito user pools", "Article CRUD UI wired to the Go API, with tag-based search", "Built and bundled with Vite, deployed as static assets to S3 behind CloudFront with a custom cache policy"] },
      { id: 3, title: "Infrastructure as Code & DevOps", task: ["Whole stack (Lambdas, API Gateway, DynamoDB, S3, CloudFront, Cognito, IAM, CloudWatch) defined in CloudFormation — environments are reproducible from a single template", "GitHub Actions pipeline: build the Go binaries, package with SAM, deploy stack updates, sync the SPA build to S3, invalidate CloudFront", "CloudWatch dashboard for Lambda invocations, error rate, p95 latency, and DynamoDB throttling"] },
    ],
    img: ["/serverless_0.png", "/serverless_1.png", "/serverless_2.png"],
    github: ["https://github.com/quentinved/epitech-crm-back", "https://github.com/quentinved/Epitech-crm-front"],
    titleref: "https://github.com/quentinved",
  },
  {
    id: 1,
    title: "Kleasy",
    type: "School",
    skills: ["React", "Go", "gRPC", "Kubernetes", "Microservices", "WebRTC", "Docker", "CI/CD"],
    description: "Production SaaS platform (kleasy.fr) that lets a business spin up a 360° cloud switchboard in one click — for cold-calling campaigns, employee numbering, and customer support. Built as part of the founding engineering team, my contribution covered the React front-end and Go/gRPC microservices behind the call-routing, contacts, and shared call-log features, deployed on Kubernetes.",
    detail: [
      { id: 1, title: "Frontend (React)", task: ["Browser-based softphone — answer & make calls from the SaaS without installing anything", "Shared address book, shared call log, and per-user time-based routing UI", "Real-time messaging surface for collaborative call handling"] },
      { id: 2, title: "Backend (Go + gRPC microservices)", task: ["Outbound, masked, and inbound call orchestration with telephone permanence (always-on routing rules)", "Blacklist + contact-sharing services with role-based access controls", "Public Open API for customers to script their own automation"] },
      { id: 3, title: "Platform", task: ["Kubernetes-deployed microservices, each service horizontally scalable independently", "Service-to-service communication over gRPC for low-latency call control"] },
    ],
    img: ["/kleasy_0.png", "/kleasy_1.png", "/kleasy_2.png"],
  },
  {
    id: 2,
    title: "Introduction to Kubernetes",
    type: "School",
    skills: ["Kubernetes", "Cloud Architecture", "Terraform", "GCP", "CI/CD", "Gitlab", "Helm", "Traefik", "Docker"],
    description: "Full-stack application (front, back, database) deployed on a multi-node GKE cluster, with the entire infrastructure provisioned by Terraform and shipped through a self-hosted GitLab CI/CD pipeline — kaniko image builds, Helm-installed Traefik ingress with HTTPS, and RBAC-scoped staging access for developers.",
    detail: [
      { id: 1, title: "Have a kubernetes with at least 2 nodes: on GCP with GKE" },
      { id: 2, title: "Deployment of this application with Terraform", task: ["Front, connected to the back and scaled to more than 1 instance", "Back, connected to the DB, with secrets mounted", "DB, with a persistent volume"] },
      { id: 3, title: "Use Helm to install Traefik (Back and Front need to be accessed through traefik with HTTPS)" },
      { id: 4, title: "Setup a Gitlab project with this CI-CD", task: ["Self hosted gitlab-runner with the Kubernetes executor and without the privileged mode enabled", "Build image with kaniko from a Dockerfile", "Testing with a Preview accessible through https/Traefik", "Deployment update using a custom RBAC"] },
      { id: 5, title: "Enable Dev to access a staging environment with Role Based Access Control." },
    ],
    img: ["/kube_0.png", "/kube_1.png", "/kube_2.png"],
    github: ["https://gitlab.com/teksf"],
    titleref: "https://gitlab.com/teksf",
  },
  {
    id: 3,
    title: "Quentinvedrenne.com",
    type: "Personal",
    skills: ["Next.js", "React", "TypeScript", "Tailwind CSS", "Framer Motion", "Cloudflare Pages", "Google Analytics 4", "Typeform"],
    description: "This site — the second iteration of my personal portfolio (v2.0), rebuilt from scratch on Next.js 14 with the App Router. Static-exported and shipped on Cloudflare Pages, fully typed in TypeScript, styled with Tailwind, and animated end-to-end with Framer Motion. Designed to keep the bar high every time someone lands here.",
    detail: [
      { id: 1, title: "Modern Next.js + React stack", task: ["Next.js 14 App Router with static export — every page pre-rendered for instant loads", "Strict TypeScript across components and data layer (PortfolioElement / Skill typed schemas)", "Reusable components: GlassCard, AnimatedSection, SkillPill, project carousel — no copy-paste"] },
      { id: 2, title: "Motion design with Framer Motion", task: ["Section-by-section reveal on scroll, staggered card entry, smooth modal transitions", "Auto-playing crossfade carousel on each project card with per-card timing offsets", "Hover-driven scale & brightness micro-interactions"] },
      { id: 3, title: "Deployment & analytics", task: ["Continuous deployment on Cloudflare Pages — every push to main is live in seconds, served from the edge", "Google Analytics 4 wired through next/script with afterInteractive strategy (no render-blocking)"] },
    ],
    img: ["/quentinvedrenne_0.png", "/quentinvedrenne_1.png", "/quentinvedrenne_2.png"],
    github: ["https://github.com/quentinved/quentinvedrenne.fr"],
    titleref: "https://quentinvedrenne.com/",
  },
  {
    id: 4,
    title: "Sharepassword",
    type: "Personal",
    skills: ["Next.js", "React", "Node.js", "Chakra UI", "Browser Extension", "REST API", "Cron"],
    description: "The first iteration of what eventually became Whisper. A web app + browser extension for sharing a password through a self-expiring link, with a Node.js API and cron-based cleanup. It worked, but it wasn't zero-knowledge — that gap is exactly what pushed me to redesign the whole thing in Rust as Whisper, with client-side encryption, a CLI, and team .env workflows.",
    detail: [
      { id: 1, title: "Web app (Next.js + Chakra UI)", task: ["Form-based password submission with configurable expiration", "Generated short share link, retrieved once and then invalidated"] },
      { id: 2, title: "REST API (Node.js)", task: ["POST: create a time-bound shareable link", "GET: redeem the link and return the stored password", "Cron job to sweep and delete expired entries"] },
      { id: 3, title: "Browser extension", task: ["Share a password and copy the share link without ever opening the website"] },
      { id: 4, title: "Lessons that drove Whisper", task: ["Server stored plaintext (or symmetric-keyed) — not zero-knowledge", "No CLI / .env workflow for teams", "All of the above is rebuilt properly in Whisper (see project above)"] },
    ],
    img: ["/sharepassword_0.png", "/sharepassword_1.png", "/sharepassword_2.png"],
    github: ["https://github.com/quentinved/SharePasswordBack", "https://github.com/quentinved/SharePasswordFront"],
    titleref: "https://github.com/quentinved/whisper",
  },
  {
    id: 5,
    title: "Area",
    type: "School",
    skills: ["Software Architecture", "React", "Node.js", "ExpressJS", "Docker"],
    description: "An IFTTT/Zapier-style automation platform built from scratch. Users connect third-party services, then compose an \"AREA\" by wiring an Action from one service to a REAction in another — the platform watches the triggers and fires the reactions automatically. React SPA, Node.js/Express API, fully containerized with docker-compose.",
    detail: [
      { id: 1, title: "Automation engine", task: ["Users subscribe to multiple external services, each exposing triggers (Actions) and effects (REActions)", "An AREA wires an Action to a REAction across services — e.g. an event in one service posts to another", "Triggers are detected automatically and reactions fire without user intervention"] },
      { id: 2, title: "Application server", task: ["Account registration with email confirmation before first use", "REST API in Node.js/Express serving the React front-end", "Service subscription and AREA composition endpoints"] },
      { id: 3, title: "One-command deployment", task: ["Front-end and back-end containerized and orchestrated with docker-compose"] },
    ],
    img: ["/area_0.png", "/area_1.png", "/area_2.png"],
  },
  {
    id: 6,
    title: "Dashboard",
    type: "School",
    skills: ["Software Architecture", "React", "Node.js", "ExpressJS", "Docker"],
    description: "A NetVibes-style personal dashboard: users subscribe to services, then build their own page out of configurable widgets, each polling fresh data on its own timer. React front-end, Node.js/Express API, containerized with docker-compose and deployed to a VPS with pm2.",
    detail: [
      { id: 1, title: "Widget system", task: ["Each subscribed service exposes a set of widgets the user can instantiate and configure", "Users compose a personal dashboard by arranging configured widget instances", "A per-widget timer refreshes the displayed data automatically"] },
      { id: 2, title: "Application server", task: ["Account registration with email confirmation before first use", "REST API in Node.js/Express managing services, widgets, and dashboard layouts"] },
      { id: 3, title: "Deployment", task: ["Front-end and back-end containerized with docker-compose", "Production deployment on a VPS, supervised by pm2"] },
    ],
    img: ["/dashboard_0.png", "/dashboard_1.png", "/dashboard_2.png"],
  },
  {
    id: 7,
    title: "Imgur Mobile App",
    type: "School",
    skills: ["Kotlin", "Mobile Application"],
    description: "Native Android client for Imgur written in Kotlin. Sign in with your Imgur account, then browse, search, filter, upload, and favorite photos — a complete consumption-and-publishing flow against the Imgur REST API, built with a classmate as a two-person team.",
    detail: [
      { id: 1, title: "Imgur API integration", task: ["OAuth authentication against the Imgur platform", "Browse the photos uploaded by the signed-in user", "Full-text photo search across the platform"] },
      { id: 2, title: "Publishing & curation", task: ["Upload photos straight from the device", "Manage favorites", "Filter the displayed photos"] },
    ],
    img: ["/imgur_0.png"],
  },
];

export default portfolioElements.filter((p) => !p.hidden);
